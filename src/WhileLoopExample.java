public class WhileLoopExample {
    public static void main(String[] args) {
        int i = 1; // Initialization
        while (i <= 5) { // Condition
            System.out.println(i); // What happens in each loop
            i++; // Iteration (make sure the loop eventually ends!)
        }
    }
}